package tests;

import tests.libs.RandomizerTestCase;

class TestRunner {
	static function main() {
        /*
		var r = new haxe.unit.TestRunner();
		r.add(new RandomizerTestCase());
        r.run();
        */
	}
}
